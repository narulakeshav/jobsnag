/**
 * External Dependencies
 */
import styled from 'styled-components';

/**
 * ListWrapper
 * @type ul
 */
export const ListWrapper = styled.ul`
  list-style: none;
  min-width: 300px;

  p {
    font-size: 14px;
    text-align: center;
  }
`;
